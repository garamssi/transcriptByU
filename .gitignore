.idea/
.claude/
*.iml
.DS_Store
docker/secrets/

# 폰트 원본 배포본(중복) — 확장이 쓰는 woff2는 fonts/에 설치됨
docs/export/Pretendard-*/
docs/export/JetBrainsMono-*/
docs/export/*.woff2

# 빌드 산출물 (build-extension.sh 결과) — 스토어 업로드용, 커밋 안 함
udemy-ai-translator-*.zip
